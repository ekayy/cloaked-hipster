<%= provide(:title, 'Search') %>
<h1>Search</h1>

<div class="row-fluid">
	<div class="span4">
	<% @user.each do |user| %>
		<%= link_to image_tag(user.profile.image.url), user_path(user) %>
	<% end %>
	</div>

</div>  